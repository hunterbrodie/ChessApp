<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core" 
             ios:Page.UseSafeArea="true"
             mc:Ignorable="d"
             x:Class="ChessApp.Pages.PlayerList">
    <ContentPage.Content>
        <Frame HasShadow="False" x:Name="listFrame" >
            <ListView x:Name="playerView" ItemSelected="playerView_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <!--<ViewCell StyleId="disclosure">
                            <StackLayout>
                                <Label Text="{Binding PName}" FontSize="Large" Margin="4, 0, 0, 0" />
                                <Label Text="{Binding Rating}" FontSize="Medium" Margin="4, 0, 0, 0" />
                            </StackLayout>
                        </ViewCell>-->
                        <TextCell Text="{Binding PName}"
                                  Detail="{Binding Rating}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Frame>
    </ContentPage.Content>
</ContentPage>