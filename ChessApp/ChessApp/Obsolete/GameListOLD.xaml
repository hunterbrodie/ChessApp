<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core" 
             ios:Page.UseSafeArea="true"
             mc:Ignorable="d"
             x:Class="ChessApp.Obsolete.GameListOLD">
    <ContentPage.Content>
        <Frame HasShadow="False" x:Name="listFrame">
            <ListView x:Name="gameView" ItemSelected="gameView_ItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Disp}"
                                  Detail="{Binding gDate}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Frame>
    </ContentPage.Content>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add Game" x:Name="AddGameToolBarText" Clicked="AddGameToolBar_Clicked" />
        <ToolbarItem Icon="add.png" x:Name="AddGameToolBarIcon" Clicked="AddGameToolBar_Clicked" />
    </ContentPage.ToolbarItems>
</ContentPage>